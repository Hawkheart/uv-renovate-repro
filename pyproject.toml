[project]
name = "demo"
version = "0.0.1"
authors = [
]
description = "Toontown Rewritten"
readme = "README.md"
classifiers = [
]
requires-python = ">=3.11"
dependencies = [
]

[dependency-groups]
dev = [
]

[project.urls]

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"

[tool.uv]
exclude-newer = "1 week"
